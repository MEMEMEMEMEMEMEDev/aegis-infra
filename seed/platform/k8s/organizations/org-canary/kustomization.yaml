apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - bundle.yaml
  - netpol.yaml   # W-07: k3s enforcea netpols nativo (kube-router
                  # embebido) — no espera Cilium (04-netpol-diseno §5)
  - routes.yaml    # #54: la ruta la pone la PLATAFORMA, no el repo de la
                  # app. Acá a mano porque el canario no tiene contrato
                  # del que derivarla; el archivo explica por qué.
generators:
  - secret-generator.yaml
