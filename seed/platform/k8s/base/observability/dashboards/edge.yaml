# «edge» dashboard — design.md §4.4: Traefik RPS/codes/latency.
# Provisioned FROM GIT via the sidecar (grafana_dashboard label —
# design.md §4.1: none is born by clicking; editable:false in the JSON).
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-borde
  namespace: observability
  labels:
    aegis.dev/part-of: aegis-platform
    grafana_dashboard: "1"
data:
  borde.json: |
    {
      "uid": "aegis-edge",
      "title": "Aegis · edge",
      "description": "The edge seen from inside: RPS, codes and latency per Traefik entrypoint.",
      "schemaVersion": 39,
      "editable": false,
      "timezone": "browser",
      "time": {
        "from": "now-24h",
        "to": "now"
      },
      "refresh": "1m",
      "tags": [
        "aegis"
      ],
      "panels": [
        {
          "id": 1,
          "title": "RPS by code (entrypoint web)",
          "type": "timeseries",
          "datasource": {
            "type": "prometheus",
            "uid": "vmsingle"
          },
          "gridPos": {
            "x": 0,
            "y": 0,
            "w": 12,
            "h": 9
          },
          "targets": [
            {
              "refId": "A",
              "expr": "sum(rate(traefik_entrypoint_requests_total{entrypoint=\"web\"}[5m])) by (code)",
              "datasource": {
                "type": "prometheus",
                "uid": "vmsingle"
              },
              "legendFormat": "{{code}}"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "reqps"
            },
            "overrides": []
          }
        },
        {
          "id": 2,
          "title": "p95 latency (entrypoint web)",
          "type": "timeseries",
          "datasource": {
            "type": "prometheus",
            "uid": "vmsingle"
          },
          "gridPos": {
            "x": 12,
            "y": 0,
            "w": 12,
            "h": 9
          },
          "targets": [
            {
              "refId": "A",
              "expr": "histogram_quantile(0.95, sum(rate(traefik_entrypoint_request_duration_seconds_bucket{entrypoint=\"web\"}[5m])) by (le))",
              "datasource": {
                "type": "prometheus",
                "uid": "vmsingle"
              },
              "legendFormat": "p95"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "s"
            },
            "overrides": []
          }
        },
        {
          "id": 3,
          "title": "4xx/5xx by service",
          "type": "timeseries",
          "datasource": {
            "type": "prometheus",
            "uid": "vmsingle"
          },
          "gridPos": {
            "x": 0,
            "y": 9,
            "w": 24,
            "h": 9
          },
          "targets": [
            {
              "refId": "A",
              "expr": "sum(rate(traefik_service_requests_total{code=~\"4..|5..\"}[5m])) by (service, code)",
              "datasource": {
                "type": "prometheus",
                "uid": "vmsingle"
              },
              "legendFormat": "{{service}} {{code}}"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "reqps"
            },
            "overrides": []
          }
        }
      ],
      "templating": {
        "list": []
      },
      "annotations": {
        "list": []
      }
    }
