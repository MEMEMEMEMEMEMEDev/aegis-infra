apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# BORN EMPTY ON PURPOSE — order as a MECHANISM, not as a comment
# (run #12): the kyverno-policies App is automated+selfHeal — the root
# creates it in phase 35 and it syncs ON ITS OWN; without this
# kustomization (directory-app) the ClusterPolicy with the live
# COSIGN_PUB placeholder went in live BEFORE the signature, its
# evaluation crashed (placeholder key + image with no signed digest)
# and the webhook with failurePolicy=Fail rejected the phase 70 canary
# ("missing digest") — an EVALUATION failure blocks even when the
# action is Audit. Phase 80 adds the entry IN THE SAME COMMIT that
# injects the real pub (runtime-entry pattern — checks 18/18b/39). Do
# NOT list policies here by hand.
resources: []
