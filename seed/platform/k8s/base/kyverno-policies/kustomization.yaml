apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NACE VACÍO A PROPÓSITO — orden como MECANISMO, no como comentario
# (corrida #12): la App kyverno-policies es automated+selfHeal — el
# root la crea en la fase 35 y sincroniza SOLA; sin este kustomization
# (directory-app) el ClusterPolicy con el placeholder COSIGN_PUB vivo
# entraba vivo ANTES de la firma, su evaluación crasheaba (clave
# placeholder + imagen sin digest firmado) y el webhook con
# failurePolicy=Fail rechazaba el canary de la fase 70 ("missing
# digest") — un fallo de EVALUACIÓN bloquea aunque el action sea
# Audit. La fase 80 agrega la entry EN EL MISMO COMMIT que inyecta la
# pub real (patrón runtime-entry — checks 18/18b/39). NO listar
# policies acá a mano.
resources: []
