apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: registry-secrets
  annotations:
    config.kubernetes.io/function: |
      exec: {path: ksops}
files:
  - registry-htpasswd.enc.yaml   # encrypted by phase 40 (A27: derived
                                 # atomically with the 4 regcred)
