apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: tunnel-secrets
  annotations:
    config.kubernetes.io/function: |
      exec: {path: ksops}
files:
  - secret-cloudflared-tunnel-token.enc.yaml  # phase 25 (tofu output
                                              # → make_enc_secret;
                                              # recreating the tunnel
                                              # => ALWAYS re-encrypt)
