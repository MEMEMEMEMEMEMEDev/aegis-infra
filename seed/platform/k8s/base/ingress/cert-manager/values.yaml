# cert-manager v1.20.2 v2 values.
crds: {enabled: true}
# A20: the chart ships hooks (startupapicheck) that ArgoCD would
# apply as PERMANENT resources — disabled:
startupapicheck: {enabled: false}
resources:
  requests: {cpu: 50m, memory: 96Mi}
  limits: {cpu: 250m, memory: 256Mi}
