apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: issuer-secrets
  annotations:
    config.kubernetes.io/function: |
      exec: {path: ksops}
files:
  - secret-cloudflare-dns-token.enc.yaml   # fase 15 (T2-E; D7: vive
                                           # SOLO acá, no en tofu)
