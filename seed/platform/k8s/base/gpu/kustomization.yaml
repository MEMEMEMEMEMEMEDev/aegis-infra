apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - runtimeclass.yaml
  - device-plugin.yaml   # order does not matter: ArgoCD sorts by kind
                         # (RuntimeClass before DaemonSet)
