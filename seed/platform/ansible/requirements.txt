# Deps of the ansible venv (created by phase 20 of init; regenerable).
# Literal pin (A12), verified against PyPI + ansible-build-data
# 2026-07-09 (bug 4, validation #3: this file was REFERENCED by phase
# 20 and did not exist → venv without ansible → k3s never ran).
#
# The "ansible" package (community) and not "ansible-core": the
# playbooks use community.general.modprobe and ansible.posix.sysctl,
# which core does NOT ship. Verified in
# ansible-build-data/14/ansible-14.0.0.deps:
#   ansible-core 2.21.0 + community.general 13.0.1 + ansible.posix 2.2.0
# Requires python >= 3.12 (Ubuntu 24.04 ships 3.12; 26.04 newer).
ansible==14.0.0
