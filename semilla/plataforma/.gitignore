# tofu: providers y state NUNCA al repo (corrida #4: git add -A
# arrastró un .terraform/ de 233 MB — GitHub lo rechaza — y el
# tfstate, que puede contener valores sensibles).
# El .terraform.lock.hcl SÍ se versiona (reproducibilidad de
# providers) — estos patrones no lo tocan.
**/.terraform/
*.tfstate
*.tfstate.backup
crash.log

# venv de ansible (regenerable de requirements.txt — fase 20):
ansible/.venv/
