apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: tunnel-secrets
  annotations:
    config.kubernetes.io/function: |
      exec: {path: ksops}
files:
  - secret-cloudflared-tunnel-token.enc.yaml  # fase 25 (tofu output
                                              # → make_enc_secret;
                                              # recrear tunnel =>
                                              # re-cifrar SIEMPRE)
