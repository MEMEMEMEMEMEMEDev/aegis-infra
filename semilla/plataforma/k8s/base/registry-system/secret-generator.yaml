apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: registry-secrets
  annotations:
    config.kubernetes.io/function: |
      exec: {path: ksops}
files:
  - registry-htpasswd.enc.yaml   # lo cifra la fase 40 (A27: derivado
                                 # atómico junto a los 4 regcred)
